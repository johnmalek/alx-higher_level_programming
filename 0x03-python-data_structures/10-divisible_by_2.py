#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    
    """
    Finds all multiples of 2 in a list
    """

    for item in my_list:
        if item % 2 == 0:
            return my_list
        else:
            return my_list
