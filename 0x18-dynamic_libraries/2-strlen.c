#include <string.h>
#include "main.h"
/**
 * _strlen - a function that returns the length of a string
 * @s: input string pointer
 * Return: integer
 */
int _strlen(char *s)
{
	return (strlen(s));
}

